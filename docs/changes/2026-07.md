## 2026-07-02 - 테스트 스크립트 민감정보 제거

### 요청 원문

> staynamcheon/tmp, 테스트 파일에 박힌 DB 연결 문자열 흔적은 삭제하고, 이미 GitHub에 올라갔다면 DB 비밀번호/토큰 회전합니다. 이건 비용보다 먼저입니다.

### 결과 요약

- `tmp`와 테스트/업로드 스크립트에 하드코딩되어 있던 DB 연결 문자열 흔적을 제거했다.
- 같은 파일에 남아 있던 Cloudinary API 키/시크릿도 환경변수 참조로 변경했다.
- 현재 HEAD 기준으로 민감값이 이미 GitHub에 올라간 파일이 확인되어 Neon DB 비밀번호와 Cloudinary API secret/key 회전이 필요하다.

### 변경 파일

- `src/scripts/test-upload.js`
- `test-final.js`
- `test-pg.js`
- `test-raw.js`
- `tmp/insert-gallery.mjs`
- `tmp/telegram-upload.mjs`
- `tmp/telegram-upload-video.mjs`
- `tmp/telegram-upload-video2.mjs`
- `docs/changes/2026-07.md`

### 검증

- 민감 패턴 검색: `postgresql://`, `postgres://`, `neondb_owner`, `ep-late-frost`, `npg_`, 기존 Cloudinary 키/시크릿 문자열 재검색 통과
- Git HEAD 검색 결과, 수정 전 민감값 포함 파일이 원격 이력에 존재함을 확인

### 커밋

- $hash Remove hardcoded test credentials
